name: Demo CI ENV Secrets Input

on:
# workflow_dispatch:
 push:
   

jobs:
    build:
        runs-on: ubuntu-latest

        steps:   
            - name: Say Hello
              run: echo Hello, world!

            - name: Install
              env:
                APP_ENV: DEMO
                DB_Pass: ${{ secrets.DB_PASS }}
              run: echo Environment $APP_ENV using secret $DB_Pass
            