name: Demo CI ENV Secrets Input


on:
 workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            
            - name: Say Hello
              run: echo Hello, world!

              - name: Install
              env:
                APP_ENV: DEMO
              run: echo Environment  $APP_ENV
            